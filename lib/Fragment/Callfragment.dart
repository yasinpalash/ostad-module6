import 'package:flutter/cupertino.dart';

class CallFragment extends StatelessWidget {
  const CallFragment ({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(

      child: Center(child: Text('CallFragment')),
    );
  }
}
